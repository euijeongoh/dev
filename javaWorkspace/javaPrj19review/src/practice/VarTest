package practice;

//변수 복습
public class VarTest {
	
	/*
	 * 변수, 선언, 할당, 초기화 가 뭔지
	 * 변수 명명규칙 : camelCase ($, _ 두 개는 가능)
	 * 주의사항 : 이름 중복 불가능
	 * 상수
	 * 변수의 스코프, 지역변수
	 * 상수, final , 재할당불가능
	 * 기본값
	 */
	
	public void test() {
		
		int x;
		x = 10;
		
		System.out.println(x);
		
		test();
	}
}
